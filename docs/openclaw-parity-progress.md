# OpenClaw Parity Progress

## Snapshot

- Estimated repo-wide parity: ~35% overall, with a reasonable band of ~30-40%
- Estimated active-family parity: ~83% for the browser/canvas/nodes/voice queue head now underway
- Read this as a planning rollup, not a generated metric or a claim of feature-complete parity

## Methodology Note

- This is a hand-scored estimate based on the parity ledger in [openclaw-parity-checkpoint-2026-04-10.md](openclaw-parity-checkpoint-2026-04-10.md), especially the 2026-04-15, 2026-04-18, and 2026-04-20/2026-04-22 updates at the tail.
- Repo-wide parity is breadth-weighted. Big unfinished families like channels, browser/canvas/nodes/voice, packaging, and companion apps pull the total down harder than the stronger control-plane work pulls it up.
- Active-family parity means the family at the current ledger tail, not the whole product. The gateway/cron/session-delivery seam is now locally closed by provider, CLI, Ops Mesh, and adjacent gateway proofs; the next active family is browser/canvas/nodes/voice.

## Feature Families

| Family | Status | Estimate | Notes |
| --- | --- | --- | --- |
| Gateway + gateway methods | Strong partial | ~92% | Method registry parity is reverified, `usage.cost` and `usage.status` are landed, `doctor.memory.dreamDiary` now provides a read-only workspace diary probe, `doctor.memory.status` now returns OpenClaw-shaped missing-memory-search diagnostics, `channels.start` now has the same validated method boundary as OpenClaw instead of falling through to unknown-method handling, `gateway.send` now has a real direct-target runtime for text plus media URL sends, `poll` rides the same explicit-target owner instead of a 503 placeholder, direct send/poll can now prefer provider-backed structured runtime callbacks, route-backed gateway webhook adapters, account/channel-specific native adapter bindings, and concrete Slack/Telegram/Discord/WhatsApp provider routes before falling back to the session-backed transcript path, provider-native result IDs now survive fresh and cached responses, saved failed `gateway/poll` replays rebuild the same poll transcript instead of collapsing to the question-only summary, direct `idempotencyKey` retries now reuse the same bounded local delivery instead of duplicating mirrored session messages, fresh/cached direct send-poll responses surface honest `runId` / `channel` plus transport metadata, native route setup is available from CLI/dashboard surfaces, object-shaped configured model primary plus fallback entries now synthesize honestly in the gateway catalog, `agents.defaults.models` allowlists now trim gateway catalogs like OpenClaw while still surfacing configured-only synthetic entries, indexed tuple-item config schema lookups plus punctuation-rich plugin entry paths now resolve on both method and HTTP surfaces, long valid config lookup paths no longer fail on an arbitrary length ceiling, `node.invoke` now blocks `system.execApprovals.*` and persistent browser-profile mutations through `browser.proxy` before wake attempts, desktop client-only nodes can pull queued system commands, `wake` can resolve the main agent target from session defaults, immediate wake retries recover after submit failures, pending node-work enqueues now wait through wake-triggered reconnect on both method and managed HTTP surfaces, and the capability inventory keeps mixed scoped plus bare-string MCP tool catalogs classified instead of dropping live plugin methods from the scope summary. |
| Cron wake/delivery | Strong partial | ~92% | Many bounded seams are closed, including wake routing, webhook delivery status, last-channel/session fallback, cron `sessionKey` wake propagation, a shared explicit-target owner, one shared outbound runtime owner spanning explicit announce plus saved session-like replays, provider-shaped send/poll callbacks, opt-in route-backed gateway webhook adapters, account/channel-specific native adapter bindings, concrete Slack/Telegram/Discord/WhatsApp provider routes, native route test/replay dispatch, Slack external uploads, Telegram URL media groups, Discord embeds/polls, WhatsApp split media/buttons, preserved provider result IDs, provider HTTP error detail, and a verified boundary split so provider-backed gateway routes no longer steal non-gateway cron failure delivery; no smaller provider-runtime blocker is currently known in this queue. |
| Onboarding + setup | Partial | ~65% | QuickStart, gateway bootstrap, re-entrant setup, mode-aware wizard state, and launch handoff are all real; disabled recurring tasks now stay staged instead of falsely launch-ready, picker-only wizard saves no longer preseed guided drafts, remote wizard sessions now persist `instance_mode="existing"` honestly, an optional saved-lane selector can pin or skip the first remote launch, switching a saved local draft to remote now clears stale pinned-lane hints before bootstrap and launch handoff while the wizard advances cleanly after answer or skip, and broken saved lane/workspace references now keep bootstrap honestly `degraded` instead of looking merely staged. |
| CLI + operator control plane | Partial | ~62% | `status`, `continue`, `queue`, recover/harden flows, gateway-doctor style surfaces, delivery replay, and native notification route creation now exist, but broad OpenClaw operator/runtime CLI breadth is still missing. |
| Routing + session identity | Partial | ~58% | Launch routing, session-key helpers, routed conversation targeting, outbox/recovery seams, and session snapshot/compaction inventory surfacing are all real now; session snapshot `limit` and `activeMinutes` also clamp to positive integers like OpenClaw, owner-session aliases from `controllerSessionKey` / `requesterSessionKey` / `spawnedBy` / `parentSessionKey` now canonicalize across filters, payloads, and child-session discovery, and `spawnedBy`-filtered key plus snapshot lookups now keep global and `unknown` sessions out of spawned-session results the same way OpenClaw does, but broader provider-owned channel/account routing is still open. |
| Skills + Ops Mesh | Partial | ~70% | Skill pins, builtin skillbooks, inbox/snapshots/inventory, and lane-aware operator supervision are real and useful. |
| Channels + direct announce delivery | Partial | ~85% | OpenZues now has routed identity, `channels.start` validation with an honest unsupported-runtime boundary, shared explicit-target owners for direct text send, media URL send, multi-media URL send, and poll, cron/session fallback delivery, route-inventory default-account reuse for explicit targets, idempotent retry collapse for the shared direct owner, saved outbound delivery views that preserve request ids, mirrored message ids, honest transport metadata, and native provider result fields, and one shared outbound runtime owner spanning direct send/poll, explicit announce, saved session-like replays, provider-shaped callbacks, opt-in route-backed gateway webhook adapters, account/channel-specific native adapter bindings, Slack Web API routes, Telegram Bot API routes/media groups, Discord webhook routes, WhatsApp Cloud API routes/split media, CLI/dashboard provider route setup, native route test/replay dispatch, provider HTTP error detail, and replay/cron boundaries reverified by full Ops Mesh plus CLI suites. |
| Browser/canvas/nodes/voice | Minimal | ~83% | Still called out as major parity gaps in the checkpoint and README, but the node lane now also allows macOS `screen.snapshot` by default while keeping `screen.record` explicitly gated, includes OpenClaw-shaped managed canvas document URL/path helpers with traversal rejection plus inline/path/PDF/remote-PDF/image materialization, non-PDF URL pass-through, manifest loading, managed asset copying, asset URL resolution, FastAPI serving, Slack media upload resolution for managed canvas paths, canvas embed shortcode parsing, control-chat canvas preview payloads/rendering, bundled A2UI scaffold serving at `/__openclaw__/a2ui`, canvas live-reload websocket accept/HTTP-upgrade boundary and reload broadcast owner at `/__openclaw__/ws`, a filesystem-backed debounce watcher that publishes canvas reload events, live-reload hook injection into served canvas HTML, scoped capability routes under `/__openclaw__/cap/<token>/...` for canvas/A2UI/WS access, A2UI JSONL validation before node dispatch, configured node allow/deny command wiring for plugin node-host commands, allowlist-normalized node list/describe/catalog/pairing command surfaces, executable gateway-method runtimes for advertised native `browser.open`, `browser.snapshot`, `browser.console`, and `browser.errors` commands, `browser.open` now opens a new tab and parses returned target metadata, `browser.status` as a read-only browser posture view, `browser.verify` as a bounded live-browser verification runtime, `browser.doctor` as posture plus optional verification, `browser.tabs` as a read-only tab inventory runtime, `browser.profiles` as a read-only profile inventory runtime, `browser.screenshot` as a controlled temp-artifact capture runtime, `browser.pdf` as a controlled temp-artifact page export runtime, `browser.get` as a read-only value bridge for text/html/value/title/url/count/box/styles/cdp-url, `browser.is` as a read-only visible/enabled/checked state bridge, `browser.stream.status` as a read-only browser stream posture bridge, `browser.diff.url` as a write-scoped URL comparison diagnostic, `browser.diff.screenshot` as a guarded visual diff diagnostic over controlled temp artifacts, `browser.download` as a write-scoped controlled temp download capture, `browser.upload` as a write-scoped guarded temp-artifact upload bridge, `browser.navigate` as a write-scoped active-session navigation runtime, `browser.back`/`browser.forward`/`browser.reload` as write-scoped history navigation methods, `browser.close` as a write-scoped session/all-session plus target-tab close runtime, `browser.focus` as a write-scoped target-tab focus runtime, `browser.start`/`browser.stop` as write-scoped lifecycle methods, `browser.act` as a bounded action bridge for wait/click/dblclick/type/fill/press/hover/focus/check/uncheck/select/scroll/scrollintoview/evaluate/resize/close, canvas host root/default page serving at `/__openclaw__/canvas`, stable-ID replacement, and manifests, pins paired-node commands to the approved roster, keeps live empty command surfaces distinct from paired approvals on catalog views, stages silent scope-upgrade requests when live commands widen or when a commandless paired node reconnects with new live commands, reuses existing silent scope-upgrade requests without read-time timestamp drift, lets desktop client-only nodes pull queued system commands, waits through wake-triggered reconnect before returning pending-work enqueue results, rejects `system.execApprovals.*` and persistent browser-profile `browser.proxy` mutations on the truthful gateway boundary before any wake attempt, keeps failed managed wakes on the truthful `node not connected` boundary, honors upstream-style `node.event` `chat.subscribe` / `chat.unsubscribe` so connected nodes can receive session-scoped downstream events, translates node `exec.started` / `exec.finished` / `exec.denied` events into session-scoped next-heartbeat system notifications with duplicate `exec.finished` suppression, converts `notifications.changed` events into session-scoped notification wakes, routes `voice.transcript` node events into the chat runtime with stable node-voice idempotency and upstream-style near-duplicate suppression, routes text `agent.request` node deep-links into the chat runtime with route-safe delivery, receipt acknowledgements, thinking, timeout hints, an honest attachment-runtime boundary, and an injectable attachment runtime adapter, lets direct `chat.send`, `sessions.send`, and `sessions.steer` use the same injected attachment runtime while preserving default unavailable boundaries, wires `create_app()` to persist effective base64 attachments under the OpenZues data dir and pass durable `media://`/hash/path metadata into control chat instead of raw blob text, recognizes recorded `push.apns.register` node events so `push.test` crosses the truthful registration boundary, exposes an injectable APNS sender adapter, wires app-level direct plus relay APNS sends with OpenClaw-style ES256 bearer tokens, bearer relay grants, gateway identity signatures, and provider response metadata, reuses recorded APNS registrations for disconnected `node.invoke` wake/reconnect attempts, retries an available APNS-backed `node.invoke` wake once when the first nudge does not reconnect, clears stale direct APNS registrations on `400 BadDeviceToken` or `410` wake/send results without clearing relay registrations, and sends a throttled foreground APNS nudge alert after background wake retry still leaves the node disconnected. |
| Packaging + companion apps | Minimal | ~5% | Still largely outside the current shipped OpenZues surface. |

## Latest Queue Head

- Live repo-level queue: [openclaw-parity-unresolved-seams.md](openclaw-parity-unresolved-seams.md)
- Queue head from the latest checkpoint tail: continue the browser/canvas/nodes/voice OpenClaw feature family after node subscriptions, exec/notification event wakes, exec/voice duplicate suppression, voice/agent ingress with route-safe delivery plus receipt acknowledgements, an honest default attachment boundary, injectable node/chat/session attachment runtime adapters, an app-level durable attachment bridge, APNS registration recognition, an injectable APNS sender adapter, app-level APNS direct/relay binding, APNS-backed `node.invoke` wake, APNS wake retry, stale direct APNS registration cleanup, throttled foreground APNS nudge, macOS `screen.snapshot` allowlist parity, managed canvas document URL/path helper parity, inline canvas document materialization, path/PDF canvas materialization, managed canvas asset copying/listing, remote PDF plus image wrapper materialization, non-PDF URL pass-through, manifest loading, FastAPI canvas document serving, Slack media upload resolution for managed canvas paths, canvas embed shortcode parsing, control-chat canvas preview rendering, A2UI scaffold serving, canvas live-reload websocket boundary, canvas host root/default page serving, canvas live-reload broadcast delivery, filesystem debounce watcher publishing, served HTML live-reload hook injection, scoped canvas capability routing, A2UI JSONL validation, configured node command allow/deny wiring, allowlist-normalized node command catalog/pairing surfaces, and native browser command method runtimes landed.
- This continuation also closed `browser.status` productization: the gateway method now returns the existing operator browser posture with method/API/policy proofs, so the next browser runtime seam shifts to `browser.verify` / `browser.doctor` or richer node-host plugin inventory.
- This continuation then closed `browser.verify` and `browser.doctor` productization: `browser.verify` now runs a bounded live-browser check through `agent-browser`, while `browser.doctor` returns browser posture with optional verification and the same honest unavailable boundary.
- This continuation then closed `browser.tabs` as the first browser command-breadth slice beyond status/verify/open/snapshot streams; the native command catalog, method dispatcher, HTTP method endpoint, and read-scope policy all expose it now.
- This continuation then closed `browser.profiles`, adding a read-only profile inventory method and native catalog entry before moving into mutating browser lifecycle/action parity.
- This continuation then closed `browser.screenshot`, adding a read-only gateway method and native catalog entry that captures an `agent-browser` screenshot to a controlled temp artifact instead of accepting arbitrary caller file paths.
- This continuation then closed `browser.pdf`, adding a read-only gateway method and native catalog entry that captures the active page as a controlled temp PDF artifact.
- This continuation then closed `browser.navigate`, adding a write-scoped gateway method and native catalog entry that navigates the active `agent-browser` session by URL while leaving target-tab routing as a later seam.
- This continuation then closed `browser.close`, adding a write-scoped gateway method and native catalog entry for session/all-session close while leaving targetId tab-close as a later seam.
- This continuation then closed the first `browser.act` slice, adding a write-scoped bounded action bridge and native catalog entry for safe local `agent-browser` action primitives while leaving target-aware tab routing and full OpenClaw action grammar as follow-on seams.
- This continuation then closed target-aware tab focus/close, adding `browser.focus` and extending `browser.close` with `targetId` while preserving session/all-session close.
- This continuation then shifted `browser.open` from active-session navigation to new-tab open semantics and added target metadata parsing; `browser.navigate` remains the active-session navigation method.
- This continuation then closed `browser.start` / `browser.stop`, adding write-scoped lifecycle methods that initialize an `agent-browser` session on `about:blank` and stop the session or all sessions through the same native gateway path.
- This continuation then corrected `browser.tabs` to use the installed `agent-browser tab list` inventory verb instead of the stale top-level `tabs` alias/help path.
- This continuation then widened the bounded `browser.act` mapper to documented local primitives: `dblclick`, `select`, `scroll`, and `scrollintoview`.
- This continuation then closed `browser.get`, adding a read-only gateway method/API/catalog/policy bridge for installed `agent-browser get <what> [selector]` values.
- This continuation then closed `browser.is`, adding a read-only gateway method/API/catalog/policy bridge for installed visible/enabled/checked state checks.
- This continuation then closed `browser.back`, `browser.forward`, and `browser.reload`, adding write-scoped history navigation methods backed by installed `agent-browser` primitives.
- This continuation then closed `browser.stream.status`, adding a read-only gateway method/API/catalog/policy bridge for installed `agent-browser stream status` output.
- This continuation then closed `browser.stream.enable` and `browser.stream.disable`, adding write-scoped gateway method/API/catalog/policy bridges for installed `agent-browser stream enable [--port]` and `stream disable`.
- This continuation then closed `browser.network.requests`, adding a read-only gateway method/API/catalog/policy bridge for installed filtered `agent-browser network requests` output without exposing `--clear`.
- This continuation then closed `browser.network.request`, adding a read-only gateway method/API/catalog/policy bridge for installed `agent-browser network request <requestId>` detail.
- This continuation then closed `browser.cookies.get` and `browser.storage.get`, adding read-only gateway method/API/catalog/policy bridges for installed cookie and local/session storage inventory without exposing set/clear mutation.
- This continuation then closed `browser.session.current` and `browser.session.list`, adding read-only gateway method/API/catalog/policy bridges for installed agent-browser session diagnostics.
- This continuation then closed `browser.diff.snapshot`, adding a read-only gateway method/API/catalog/policy bridge for installed snapshot diff diagnostics without exposing arbitrary baseline file paths.
- This continuation then closed `browser.auth.list` and `browser.auth.show`, adding read-only gateway method/API/catalog/policy bridges for installed auth profile metadata without exposing save/login/delete or passwords.
- This continuation then closed `browser.diff.url`, adding a write-scoped gateway method/API/catalog/policy bridge for installed URL comparison diagnostics without exposing arbitrary baseline or output file paths.
- This continuation then closed `browser.diff.screenshot`, adding a read-scoped guarded visual diff bridge that accepts only OpenZues temp screenshot baselines and writes only controlled temp diff artifacts.
- This continuation then closed `browser.download`, adding a write-scoped controlled download bridge that clicks a selector and writes only to OpenZues-generated temp artifacts.
- This continuation then closed `browser.upload`, adding a write-scoped guarded upload bridge that accepts only existing OpenZues temp artifacts.
- This run integrated the adjacent hot gateway model/session/config-schema shard already living in the dirty tree: `GatewayModelsService` now filters catalogs through `agents.defaults.models` allowlists like OpenClaw while still surfacing configured-only synthetic entries, `GatewaySessionsService` now keeps `spawnedBy` key and snapshot lookups off global plus `unknown` sessions instead of trusting metadata-only matches, and `config.schema.lookup` no longer rejects long but otherwise valid paths on a synthetic length cap.
- Queue head narrowed again: durable app-level base64 attachment storage/offload is now closed for direct chat/session gateway entrypoints. The next browser/canvas/nodes/voice seam should move to the next concrete upstream canvas/media or browser node surface instead of reworking the already-persisted text-envelope bridge.
- Honest boundary: OpenZues now carries cron `sessionKey` through queued wakes, replays saved direct deliveries with honest transport identity, rebuilds saved failed `gateway/poll` deliveries into the same bounded local poll transcript, keeps launch/session and Ops Mesh channel-account identity on the same canonical account/peer rules, derives known default accounts from route inventory before shared explicit-target delivery, routes `gateway.send` direct text plus media URL messages through that owner, routes Telegram multi-media URL sends through `sendMediaGroup`, splits WhatsApp multi-media URL sends into separate image messages, routes `poll` through the same explicit-target delivery path, routes explicit announce plus saved session-like outbound delivery replays through the same shared outbound runtime owner, dedupes repeated direct send/poll retries by `idempotencyKey` on that shared local owner, prefers provider-backed structured send/poll callbacks when adapters are supplied, can use notification routes that explicitly subscribe to `gateway/send` or `gateway/poll` as route-backed provider adapters, can bind account/channel-specific native message and poll adapters ahead of generic provider/session fallback, can deliver Slack/Telegram/Discord/WhatsApp native route payloads, exposes native route setup on CLI/dashboard surfaces, dispatches saved native route tests/replays through provider-native send/poll payloads, surfaces provider HTTP error body detail in failures, surfaces honest `runId` / `channel` plus transport and provider result metadata on fresh and cached direct gateway responses, preserves the same transport metadata and provider result fields on saved outbound delivery views, keeps gateway session discovery/compaction inventory honest without inventing an empty fallback global session, now also honors object-shaped configured model primary plus fallback entries, `agents.defaults.models` allowlist filtering, OpenClaw-style session filter clamping, spawned-session visibility on global/unknown rows, owner-session alias canonicalization, remote-saved-lane wizard state, bootstrap repair degradation for broken saved defaults, punctuation-rich plus long config lookup ids, `node.invoke` guard rails for `system.execApprovals.*` and persistent browser-profile `browser.proxy` mutations, wake-triggered pending-work reconnect waits, node `agent.request` effective attachments through an injected attachment runtime while default installs keep the explicit unavailable boundary, app-level `chat.send` / `sessions.send` / `sessions.steer` base64 attachments persist to durable local media files and enter chat as bounded metadata instead of raw base64, app-level `push.test` direct/relay sends through OpenClaw-compatible APNS requests when direct credentials or a relay URL are configured, disconnected APNS-registered nodes can wake/reconnect before `node.invoke` with one retry if the first available APNS wake does not reconnect, stale direct APNS registrations are cleared when APNS returns `400 BadDeviceToken` or `410` on alert or wake sends, and disconnected APNS nodes get one throttled foreground reopen nudge when background wakes still fail to reconnect.
- The former hot session archive/compaction inventory surfacing seam is now locked, so there is no shorter hot-shard detour ahead of the provider-runtime queue head.
- There is no separate session-backed `gateway.send` breadth placeholder left ahead of the queue head; direct text, poll, media URL send, Telegram multi-media URL send, WhatsApp split media send, explicit announce, saved session-like replays, provider-shaped callbacks, opt-in route-backed gateway webhooks, provider result metadata, native adapter binding, Slack/Telegram/Discord/WhatsApp native routes, native route setup, native route test/replay dispatch, and provider HTTP error detail now share the same local runtime owner.

## References

- Primary ledger: [openclaw-parity-checkpoint-2026-04-10.md](openclaw-parity-checkpoint-2026-04-10.md)
- Repo-level seam queue: [openclaw-parity-unresolved-seams.md](openclaw-parity-unresolved-seams.md)
- Useful narrow seam example: [openclaw-parity-relay-2026-04-14-recovery-thread-019d8e51.md](openclaw-parity-relay-2026-04-14-recovery-thread-019d8e51.md)
